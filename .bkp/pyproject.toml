[project]
name = "rdf-code-generate"
version = "0.1.0"
description = "Generate TypeScript types from RDF/Turtle vocabularies, with a focus on schema.org."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rdflib>=7.6.0",
]
license = {text = "MIT"}